# Jim Fan × Molly Welch：《机器人与具身智能的未来》（The Future of Robotics and Embodied AI）
## 要点与清洗编辑版长篇整理稿

> 节目：Radical Talks, Masterclass Edition  
> 发布时间：2026年2月11日  
> 时长：约45分钟  
> 主持人：Molly Welch  
> 嘉宾：Jim Fan，NVIDIA总监、杰出研究科学家，GEAR / Project GR00T负责人

---

# 一、文档说明

这篇与Sergey、Nikita两篇不同：官方公开的是高质量长摘要和章节结构，而不是完整逐字文字稿。因此本文采用“**官方节目内容 + 同期公开演讲 + NVIDIA GR00T / Cosmos / Isaac官方资料**”进行交叉重构，保留节目明确讨论的技术主线，但不虚构逐字对白。

整理重点：

1. Jim Fan的研究哲学：**数据最大化、模型最简化（Data Maximalist, Model Minimalist）**。
2. 为什么Embodied AI的主要瓶颈不是再放大模型，而是数据、探索和仿真。
3. 为什么仿真 / 合成数据 / 世界模型是NVIDIA路线的核心。
4. GR00T为什么强调端到端、通用型和人形机器人。
5. 机器人“GPT-4 Moment”真正需要什么。
6. 2026年内从GR00T N1.6 → GR00T 1.7的后续变化。
7. 文末补充从VLA、数据闭环与机器人质检视角的解读。

---

# 二、核心观点

## 1. Jim Fan认为，机器人并不是“再训练一个更大的模型”就会自然解决

语言模型近年的进步很大程度来自：

**数据、算力与可扩展架构。**

但机器人不能简单复制这条路径，因为它缺少文本互联网那样的天然数据矿藏。

真正卡住Embodied AI的不是“模型还不够大”，而是：

- 真实交互数据稀缺；
- 机器人采集慢；
- 失败成本高；
- 真实环境不能无限并行；
- 任务和本体极度多样。

所以他认为机器人下一阶段更应该优化：

**经验规模化。**

---

## 2. 机器人真正缺少的是“互联网级经验”

LLM可以吃：

- 网页；
- 书；
- 代码；
- 图文；
- 对话。

机器人需要的数据却是：

- RGB / Depth；
- Joint状态；
- 动作；
- Force；
- 接触；
- 环境反馈；
- 任务结果。

这些数据连续、高维、时序强相关，而且必须真实执行才能产生。

遥操作虽然质量高，但受制于：

- 人工；
- 机器数量；
- 复位；
- 硬件故障；
- 时间。

所以人类示范不能单独承担通用机器人的规模化训练。

---

## 3. Jim的核心研究哲学是：数据最大化主义，模型最简主义

模型应该尽量保持简单：

**像素 / 状态 / 语言 → 动作。**

复杂性主要放在数据和训练系统里，而不是不断堆手工模块。

他长期偏好：

- 简洁接口；
- 端到端；
- 能随数据规模扩展的架构；
- 少量人为规则。

这和他早期受到AlexNet的影响一致：

复杂手工视觉流水线最终被简单的端到端学习取代。

---

## 4. 端到端的价值不是“结构越少越优雅”，而是更容易随数据和计算扩展

传统机器人技术栈通常包含：

- 感知；
- 建图；
- 目标检测；
- 跟踪；
- 运动规划；
- 控制器；
- 手工状态机。

每个模块都有人为假设和接口。

模型越复杂，新增数据也不一定能自动改善整个系统。

Jim更看重：

**一个统一学习目标能不能把更多经验转化为更强行为。**

如果可以，数据和算力才真正具有规模化价值。

---

## 5. 机器人数据瓶颈的真正解决方案，必须大量依靠合成经验

真实数据不会消失。

但它更适合充当：

**种子数据 / 现实锚定数据。**

随后通过：

- 物理仿真；
- 域随机化；
- 合成轨迹；
- 世界模型；
- 生成式视频；

把少量真实数据扩展成更大训练分布。

目标不是替换现实，而是：

**放大真实世界数据。**

---

## 6. 物理仿真的最大优势，是可以并行做强化学习

真实机器人：

一次失败可能意味着：

- 机器人摔倒；
- 零件损坏；
- 人来复位；
- 环境需要重新整理。

仿真可以：

- 数千环境并行；
- 快速复位；
- 无硬件风险；
- 重复探索；
- 系统性改物理参数。

因此对于移动控制、全身控制等技能，仿真 + RL仍是当前最可扩展路线之一。

---

## 7. 域随机化的本质，是让策略不依赖某一个“完美仿真世界”

仿真不可能精确等于现实。

所以训练时主动改变：

- 摩擦；
- 质量；
- 重力；
- Sensor Noise；
- Dynamics；

可以让策略学会一组鲁棒策略。

真正目标不是：

**做一个绝对正确的仿真器。**

而是：

**让策略对现实中合理变化不敏感。**

---

## 8. 但Jim也认为传统仿真本身不够，因为现实世界的视觉和场景多样性太大

物理引擎擅长：

- 接触；
- Dynamics；
- Motion；
- 碰撞。

却很难廉价生成：

- 数百万家庭；
- 数百万物体；
- 材质；
- 光照；
- 人类行为；
- 开放世界组合。

这就是世界模型 / 生成模型进入Robotics的原因。

---

## 9. 世界模型的价值，是充当“反事实仿真器”

Jim把世界模型看作：

在当前状态和动作条件下，预测未来世界会怎样变化。

这让智能体可以：

- 预演；
- 比较动作；
- 生成合成轨迹；
- 扩展训练分布。

与传统仿真器不同，世界模型可以吸收大规模视觉数据中的外观先验和Physical先验。

---

## 10. GR00T Dreams的核心逻辑：真实机器人数据是种子，生成模型把它扩成更大的训练集

NVIDIA的路线不是：

“未来全部用合成数据。”

而是：

真实示范数据  
→ 微调世界模型  
→ 生成更多场景 / 轨迹  
→ 和真实数据一起训练VLA

这样高成本真机数据承担：

**现实锚定。**

合成数据承担：

**规模化。**

---

## 11. Jim认为真正值得研究的是探索，而不只是模仿学习

人类示范的限制是：

模型只能看到人已经决定展示的东西。

真正的智能还需要：

- 主动尝试；
- 发现失败；
- 改变策略；
- 搜索新行为。

Minecraft的Voyager、Eureka等工作，本质都在研究：

**基础模型 + 智能体 + 反馈 → 自主扩展能力。**

这条思想后来被迁移到物理AI。

---

## 12. Voyager的意义，不是Minecraft游戏本身，而是“自动产生课程式学习和技能库”

一个通用智能体不应该完全依赖研究者手工规定：

第一步学什么；
第二步学什么；
第三步学什么。

Agent可以根据当前能力：

- 选择下一目标；
- 写代码；
- 执行；
- 看反馈；
- 把成功策略保存进技能库。

Jim认为具身智能体最终也需要这种：

**开放式技能获取。**

---

## 13. Eureka进一步说明，LLM可以帮助RL自动设计Reward

传统机器人RL很耗专家时间的一件事是：

**奖励设计。**

Eureka展示：

LLM可以生成并改进奖励函数，再用真实训练结果反向筛选。

这意味着基础模型不只是策略，也可以成为：

- 奖励设计器；
- 课程式学习Designer；
- 数据Generator；
- Evaluator。

---

## 14. Jim对具身的核心判断：被动观察不等于主动学习

他长期引用“两个小猫”实验：

两只小猫看到几乎相同视觉输入。

一只能主动移动，一只被动跟随。

只有主动控制自己身体的那只发展出正常视觉-运动能力。

Jim借此强调：

**动作和主动性本身会改变表征学习。**

所以仅仅把整个YouTube喂给模型，不一定等价于拥有真正的物理智能。

---

## 15. “主动小猫”（Active Kitten）是Jim理解AGI的关键隐喻

今天最强的LLM很像一个极强的“被动小猫”：

看过世界大量数字记录，却没有真正通过身体作用于世界。

Jim认为更完整的Intelligence需要：

- Experiment；
- 动作；
- 失败；
- Feedback；
- Retry。

具身不是AI的一个附属应用，而可能是更完整智能的重要组成部分。

---

## 16. 为什么NVIDIA重点做人形机器人？第一，因为“我们知道这种身体形态是有效的”

人形机器人的优势非常现实：

- 人类已经证明这副身体能完成绝大多数经济劳动；
- 工具、楼梯、桌椅、门把手都为人设计；
- 人能自然预测人形机器的动作；
- 与人共享环境更直观。

所以人形是一个高价值通用Hardware先验。

---

## 17. 但GR00T的真正目标仍是通用型能力，不是只做一个固定的人形机器人技能

人形机器人只是当前最重要载体之一。

GR00T更大的目标是建立：

- 通用视觉语言理解；
- 全身动作；
- 多本体；
- 任务泛化；
- 可迁移的Foundation策略。

后续GR00T公开版本也逐步增加不同机器人具身。

---

## 18. Jim对机器人智能的“快慢系统”理解：高层推理与低层运动控制必须共存

机器人既需要：

### Slow / 系统2
- 语言；
- 规划；
- 语义推理；
- Long Horizon。

也需要：

### Fast / 系统1
- 感知；
- 接触；
- Balance；
- Reflex；
- 运动动作。

近几年LLM极大改善了系统2。

物理AI的最大缺口仍在：

**快系统（System 1）的数据与技能。**

---

## 19. GR00T的路线就是让高层推理和快动作在统一系统里协同

公开的GR00T N系列逐渐形成：

- VLM Backbone；
- 机器人状态；
- 动作专家模块 / 扩散Transformer；
- 动作块；
- 全身技能。

其目标不是让一个大语言模型逐Token输出关节角，而是：

**语义理解与高频运动策略在统一模型里共享表征。**

---

## 20. Jim对“机器人GPT-4 Moment”的判断，不看某个单一基准测试

他更关心：

- 数据流水线是否可以规模化；
- 机器人基础模型是否能跨本体和任务；
- 合成数据是否真正提高真机能力；
- 部署是否从演示进入持续工作。

所以“GPT-4 Moment”不是：

一个机器人在发布会上做了特别炫的动作。

而是：

**机器人系统开始进入可持续的能力复利。**

---

## 21. 最早产生商业价值的场景，大概率不是家庭

Jim更看好：

- 可编程工厂；
- 自动化实验室；
- 结构化工业环境。

这些场景：

- 任务更重复；
- 安全边界清楚；
- 环境可设计；
- 成本收益明确。

家庭则有：

- 儿童；
- 宠物；
- 隐私；
- 个性化；
- 极端长尾；

因此更晚。

---

## 22. “可编程工厂”与传统工业自动化的区别，是任务切换从工程编程变成示范 / 学习

传统自动化：

每个新Task都要工程师重新写：

- 轨迹；
- PLC；
- 状态机；
- Fixture。

Foundation机器人更理想的方式是：

- 提供示范数据；
- 给自然语言；
- 少量微调；
- 快速换任务。

所以真正的产业价值不是“机器人动作更炫”，而是：

**重新编程成本大幅下降。**

---

## 23. 自动化科研可能是一个被低估的早期物理AI场景

实验室具有：

- 任务结构较清晰；
- 器材标准化；
- 价值密度高；
- 对24/7运行有明确需求。

机器人 + AI科研智能体可以：

- 准备实验；
- 操作仪器；
- 观察结果；
- 自动迭代。

这可能比普通家庭更早形成真正自主智能体驱动的物理工作流。

---

## 24. Jim的时间观很长期：物理AI会经历“慢慢积累，然后突然看起来不可逆”

他不愿过度押一个精确年份，但对长期非常乐观。

他曾给出一个非常激进的判断：

到2040年，智能机器人数量可能超过iPhone。

重点并不在这个数字本身，而是他认为机器人会从：

**几乎为零的通用市场**

走向：

**像个人计算一样广泛的计算平台。**

---

# 三、清洗编辑版长篇整理

## 第一章：为什么我一直偏爱简单、端到端的方法

**Molly：**你从OpenAI、Stanford到NVIDIA，研究方向变化很多，有什么一直没变？

**Jim：**我一直很受AlexNet那种思想吸引。

以前计算机视觉有巨大手工流水线。

AlexNet把它简化成：

像素输入 → 标签输出。

复杂度转移到数据和算力。

我后来做智能体、机器人研究也一直喜欢这种路线。

系统越复杂，我越希望模型Interface越简单。

---

## 第二章：机器人为什么比语言模型难得多

语言模型的数据几乎已经存在。

机器人数据必须被创造出来。

每一次：

- Joint Motion；
- 接触；
- 操作；
- 失败

都需要真实或模拟系统运行。

所以我们没有互联网那种“免费几万亿Token”。

我觉得机器人的根本问题首先是：

怎样找到互联网规模的交互经验。

---

## 第三章：遥操作很重要，但它不可能无限规模化

人类示范是非常有价值的现实锚定。

但一台机器人一天真正能收多少高质量数据？

硬件要复位、会坏、人也会累。

所以最终一定需要：

**Real数据 + 仿真 + 合成经验。**

不是三选一。

---

## 第四章：为什么仿真对机器人特别关键

仿真最大的优势是：

Parallelism。

你可以同时跑成千上万个机器人。

失败后瞬间复位。

对移动控制、全身控制这样的任务，RL在Sim中可以做现实完全负担不起的探索。

但必须解决Sim-to-Real。

---

## 第五章：域随机化的意义

我们无法让仿真器完美复制真实世界。

所以与其幻想一个完美世界，不如在Training时主动改变物理参数。

这样策略不会依赖某个精确摩擦系数或质量。

但随机化也不能无限乱加。

最终要围绕真实世界可能变化的分布。

---

## 第六章：世界模型是下一层合成数据引擎

传统仿真器很难创造海量真实视觉世界。

Generative世界模型可以从真实视频中吸收：

- 外观先验；
- 物体多样性；
- Scene先验。

然后在动作条件下预测未来。

这使它可以成为另一类仿真器。

它不是为了做漂亮视频。

它是为了让机器人产生更多可训练经验。

---

## 第七章：真实数据是种子，合成数据是放大器

我们不应该争论：

真实数据还是合成数据？

真正正确的问题是：

怎么用最昂贵的真实数据对系统做现实锚定，再用仿真和世界模型把它扩大几个数量级？

真实世界负责Truth。

合成数据系统负责规模化。

---

## 第八章：为什么探索比模仿学习更接近Intelligence

只模仿Demonstration，会被人类示范覆盖范围限制。

真正的智能体应该：

- 尝试；
- 失败；
- 修改；
- 发现新技能。

我们在Minecraft做Voyager，本质就是想研究开放式学习。

它会自己选目标、编写技能、执行、积累技能库。

机器人最终也要有类似能力。

---

## 第九章：基础模型甚至可以帮RL设计Reward

Eureka做了一件很有趣的事：

让LLM写奖励函数。

然后真正跑RL。

看哪些奖励产生更好的行为。

再自动迭代。

这意味着大模型不仅能当策略。

也可以参与：

- Reward；
- 课程式学习；
- 评测。

机器人训练系统本身也会越来越智能体化。

---

## 第十章：为什么“两个小猫”一直影响我

两个小猫看到相同视觉世界。

一个主动移动，一个只是被动跟着。

最后只有主动那个形成正常的视觉运动能力。

这说明：

主动性本身就是学习信号。

今天大模型看了很多世界，但大部分是被动看。

真正的物理AI需要：

动作 → 后果 → Learning。

---

## 第十一章：为什么押人形机器人

人形机器人不是因为它最漂亮。

而是因为我们知道：

它能完成大量经济劳动。

环境也是按这种Body设计的。

而且人和它协作时更容易理解它会怎么动。

这是一个很强的Hardware先验。

---

## 第十二章：但最终我们要的是通用型Brain

GR00T不是某个动作模型。

我们希望它吸收：

- 视觉；
- 语言；
- 本体感知；
- 人类视频；
- 仿真；
- 机器人示范数据；

然后输出全身动作。

模型需要适配不同机器人和Task。

人形机器人是重要入口，不是智能的最终定义。

---

## 第十三章：系统2已经很强，系统1仍然缺数据

LLM已经非常擅长：

- 推理；
- 编程；
- 规划。

但Physical系统还不会像人一样自然地：

- 抓；
- 走；
- 接触；
- 平衡。

这些“直觉动作”是系统1。

真正Embodied AI最大的数据缺口就在这里。

---

## 第十四章：机器人真正的GPT Moment是数据飞轮

我不会把某个发布会定义成GPT-4 Moment。

更重要的是：

我们有没有找到一套系统，使得：

更多经验  
→ 更强模型  
→ 更好部署  
→ 又产生更多经验。

一旦这个循环成立，之后的进展会突然显得非常快。

---

## 第十五章：为什么先从Factory和Lab开始

家庭太难。

Factory和Lab可以：

- 约束环境；
- 控制安全边界；
- 量化收益；
- 反复执行。

而且基础模型最大价值是：

新任务不再需要工程团队写几个月程序。

演示一次，模型就能开始适配。

这才是“可编程机器人”的真正意义。

---

# 四、技术核验：Jim Fan的2026观点如何对应NVIDIA公开路线

## 1. GR00T N1.6：VLA + 推理 + 扩散Transformer

2026年初公开的N1.6已经把：

- VLM；
- 机器人状态；
- 全身Diffusion策略；
- Sim-to-Real RL；
- 合成导航数据；

放进完整人形机器人工作流。

说明节目中“快慢智能 + 端到端运动控制”并不是抽象愿景。

## 2. GR00T 1.7：数据继续扩大，而且更明确地把人类视频 + 仿真一起纳入预训练

截至2026年7月，NVIDIA公开GR00T 1.7：

- 约32K小时真实机器人示范 / 人类第一人称数据；
- 约8K小时仿真；
- 3B基础模型检查点；
- 更强长时程任务分解；
- TensorRT / ONNX部署。

这非常直接地体现：

**数据最大化主义。**

## 3. NVIDIA的工具链不是只做模型，而是把数据 → 仿真 → 训练 → 评测 → 部署做成平台

Isaac GR00T平台继续整合：

- Isaac Sim；
- Isaac Lab；
- Teleop；
- Arena评测；
- Cosmos；
- OSMO；
- TensorRT。

这和Jim的观点一致：

机器人领域真正的规模化问题是系统工程，而不是单论文模型。

---

# 五、从VLA / 数据质检视角的延伸解读

## 1. 仿真数据不能只检查“生成成功”，要检查Sim分布是否合理

域随机化如果过宽，策略会学得过度保守。

如果过窄，又无法覆盖真实世界。

所以仿真QA必须关注：

- 参数分布；
- 场景合理性；
- 接触；
- Sensor Noise；
- 真实世界标定。

## 2. 合成数据的质量标准最终是“是否提升Real策略”

世界模型生成的视频看起来逼真，不等于训练价值高。

最终指标应该是：

**加入合成数据训练 → 真实世界评测提升。**

这是比FID、视频美观度更重要的机器人指标。

## 3. Jim的路线天然需要数据来源追踪

模型会混合：

- Real Teleop；
- 人类视频；
- Sim；
- 生成式执行轨迹。

数据平台应明确记录：

- Source；
- Fidelity；
- 物理参数设置；
- Generation模型；
- 机器人具身；
- 奖励 / 成功结果；
- 是否经过真实环境标定。

否则后续无法判断哪个数据Source真正有效。

## 4. 探索数据与示范数据不能用同一套质检逻辑

示范数据追求：

- 成功；
- 平滑；
- 合理策略。

探索数据允许：

- 失败；
- 极端状态；
- 新尝试。

如果用“演示Quality”规则清洗探索，会把最有价值的边界样本删掉。

## 5. 合成数据规模化最终必须与Real现实锚定闭环

理想流程更像：

**真实数据 → 仿真器 / 世界模型标定 → 合成扩增 → 策略训练 → 真实环境评测 → 失败数据 → 重新标定**

而不是：

**建一个Sim → 无限生成 → 训练结束。**

---

# 六、截至2026年9月回看

## 1. Jim 2月强调的数据与仿真路线，2026年后续继续强化

GR00T 1.7明确增加真实、人类第一人称、模拟rollout的混合预训练，并强化端到端开发工具。

## 2. 全身控制仍在持续进展

NVIDIA GEAR 2026年公开SONIC等全身运动跟踪工作，说明快系统（System 1） / 全身技能仍然是重点，而不是被LLM推理取代。

## 3. “世界模型 = 数据放大器”已经成为NVIDIA物理AI平台固定组成

Cosmos / GR00T-Dreams / 动作条件视频预测都继续沿这条方向推进。

因此这期访谈更像对NVIDIA 2026技术路线的一次方法论总结，而不是孤立观点。

---

# 七、资料来源与可靠性说明

主要依据：

1. Radical Talks：Jim Fan on The Future of Robotics and Embodied AI，2026-02-11。
2. Radical Ventures官方长文摘要。
3. Apple Podcasts官方节目页与时长。
4. Jim Fan同期公开的GR00T / 具身智能演讲，用于核对其长期研究观点。
5. NVIDIA Isaac GR00T N1.6 / GR00T 1.7 / Cosmos / Isaac Lab官方资料。
6. Voyager、Eureka、GR00T N1等公开研究背景。
7. 本文为编辑重构，不声称是逐字Transcript。

---

# 八、一句话概括

> **Jim Fan的核心路线可以压缩成一句话：机器人不缺“再大一点的脑子”，真正缺的是可无限扩张的经验；真实数据负责现实锚定，仿真和世界模型负责规模化，而模型本身应尽可能保持简单、端到端，并让智能从海量、多样、可探索的数据中长出来。**
