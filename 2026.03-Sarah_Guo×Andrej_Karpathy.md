# Andrej Karpathy × Sarah Guo：Code Agents、AutoResearch 与“Loopy”AI 时代
## 核心观点 + 清洗后的长篇访谈稿 + 技术核验与解读（重制版）

> 节目：No Priors  
> 原题：Andrej Karpathy on Code Agents, AutoResearch, and the Loopy Era of AI  
> 发布时间：2026 年 3 月 20 日  
> 时长：约 1 小时 07 分  
> 主持人：Sarah Guo  
> 嘉宾：Andrej Karpathy

---

# 一、文档说明

这份重制版按照“**尽量保留有效信息，只删除口水、重复和无关枝节**”的标准整理，而不是把十分钟内容压成三句话。

全文严格区分三层：

1. **原访谈观点**：只整理 Karpathy 和 Sarah Guo 在这期原节目里实际讨论过的内容。
2. **技术核验与背景**：只用于补足访谈里提到但没有展开的项目或概念，例如 AutoResearch、microgpt；不会倒灌进“原访谈观点”。
3. **我的解读**：明确标注为整理者分析，不视为 Karpathy 原话。

“清洗后的长篇访谈稿”不是逐字翻译，而是对原对话进行中文化、去重复和结构重排后的**高保真转述**。保留案例、追问、限制条件、反例、因果链和重要旁枝；不为了追求“像全文”而虚构问答。

---

# 二、原访谈核心观点

> 本节是高压缩摘要。想真正理解论证，请看第三部分长篇正文。

## 1. Coding Agent 带来的不是补全质量提升，而是软件工作的基本粒度发生了变化

Karpathy 说，自己的工作方式在 2025 年 12 月附近出现了明显转折：从“主要亲自写代码、少量委托 Agent”，快速变成“主要委托 Agent，自己几乎不直接写代码”。

人的工作从：

- 写一行代码；
- 写一个函数；

上移到：

- 分配一个完整功能；
- 同时安排研究、实现和方案设计；
- 在多个代码仓库或 worktree 之间调度；
- 检查结果并决定下一轮动作。

他把这些称作对代码库的更大粒度“宏动作”。

## 2. 多 Agent 并不是必须的最佳实践，而是 Karpathy 正在探索的新“并行度”

他看到 Peter Steinberger 同时运行很多 Codex 会话，每个会话可能持续约二十分钟，并在多个 checkout 之间分配互不干扰的任务。

Karpathy 因此开始追问：当一个 Agent 在工作时，为什么自己还要等？如果还有独立任务和可用 Token，是否应该继续启动其他 Agent？

他的表达明显带有“极端 power user 探索”的意味，并不是在给普通工程师规定标准流程。

## 3. 他认为人的新瓶颈可能从算力变成“能调度多少 Token”

Karpathy 把这种感觉类比成读博时看到 GPU 空闲会焦虑：过去担心没有把 FLOPs 用满，现在会担心没有把 Agent / Token 吞吐量用满。

他反复说“skill issue”，意思是很多失败看起来不像模型完全没有能力，而像自己还没有：

- 给出合适指令；
- 设计足够好的 Memory；
- 找到正确的并行拆分；
- 组合合适工具。

## 4. 普通 Coding Agent 之上，会出现更持久的“Claw”式 Agent

Karpathy 用“Claw”指一类比普通一次性会话更持久的 Agent：

- 有自己的沙箱；
- 不需要人持续盯着；
- 可以循环执行；
- 有更复杂的长期记忆；
- 有固定消息入口；
- 甚至具有较稳定的人格。

OpenClaw 对他有吸引力，不是因为某一个功能，而是它同时把多项 Agent 产品设计组合在一起。

## 5. 长期 Agent 的“人格”不是纯装饰

Karpathy 专门讨论了不同 Coding Agent 给人的“队友感”。

他觉得有些 Agent 过于干，只有“已完成”的机械反馈；Claude 在他看来更像一个共同工作的队友，而且赞扬并非每次都一样强烈。他甚至开玩笑说，自己会产生“想赢得 Claude 表扬”的感觉。

他的真正观点是：如果 Agent 会长期陪伴人工作，**交互人格会成为产品体验的一部分**，而不仅仅是模型正确率之外的无关包装。

## 6. Dobby 家庭 Agent 让他开始认真思考“Agent-first 软件”

Karpathy 用 Agent 做了一个家庭常驻系统 Dobby。

他没有手工配置每个智能设备，而是让 Agent：

- 扫描局域网；
- 找到 Sonos；
- 研究设备接口；
- 查 API；
- 尝试控制；
- 再逐步接入灯光、暖通、遮阳、泳池 / Spa 和摄像头等系统。

安防摄像头发生变化时，还可以经过视觉模型判断事件并主动发消息。

这让他产生一个更大的产品判断：很多专用 App 也许不必存在，设备和服务更应该暴露稳定 API，由代表用户行动的 Agent 充当智能“胶水”。

## 7. 他同时对常驻 Agent 的权限很谨慎

尽管 Dobby 很有趣，Karpathy 并没有把邮件、日历等整个数字生活都交给它。

原因是：

- 安全；
- 隐私；
- 系统仍然粗糙；
- 他还不愿给过高权限。

这点很重要：他对常驻 Agent 的兴奋，不等于他认为今天已经适合完全无人监管地授权一切。

## 8. AutoResearch 的目标是“把研究者移出实验闭环”

Karpathy 的核心问题不是“Agent 会不会提出科研点子”，而是：

**能不能把研究流程改造成：给目标、指标和边界，然后 Agent 自己反复修改、训练、评估和保留改进，人不再负责每一轮续 Prompt。**

他让 AutoResearch 在自己已经调得相当充分的小型 LLM 训练环境上跑了一夜，仍然找到了若干他没有继续调到的优化组合，这让他对自动研究的可行性明显更乐观。

## 9. AutoResearch 的真正适用边界是“搜索昂贵、验证便宜”

Karpathy 非常强调客观指标。

适合 Agent 自主闭环的任务通常有：

- 可自动运行；
- 有清晰指标；
- 失败可回滚；
- 一个候选方案容易复验。

CUDA Kernel 性能优化、训练损失优化等都是理想场景。

难以验证的软判断、审美、幽默等领域，则没有同样强的闭环优势。

## 10. 他进一步设想“研究组织本身也可以被代码化”

`program.md` 在他看来不只是 Prompt，而可以描述：

- 研究者角色；
- 点子从哪里来；
- Worker 怎样取任务；
- 怎样验证；
- 怎样合并。

如果不同 `program.md` 会导致不同研究效率，那么“研究组织怎么运作”本身也可以成为被优化的对象。

## 11. 当前模型依然是高度“锯齿状”的智能

Karpathy 一方面说 Agent 能连续几小时“搬山”，另一方面指出模型仍会讲五年前一样的烂笑话。

他借此反对一个过于简单的假设：

> 代码、数学这些可验证领域变强，并不会自动、同步地让所有能力一起变聪明。

很多能力是否提升，取决于它有没有被训练、强化和评测。

## 12. 长期可能出现模型“物种分化”

目前前沿实验室大多追求一个模型覆盖所有任务。

Karpathy 认为未来可能出现更多专门化模型：

- 保留通用认知核心；
- 在数学、Lean、代码等领域强化；
- 换取更低延迟和更高吞吐量。

但他同时指出，今天“怎样改权重而不破坏其他能力”仍不是成熟科学，所以这种模型分化还没有真正大规模展开。

## 13. 他设想开放的 Agent Swarm 参与研究

AutoResearch 的候选改进具有一个有趣性质：

- 找到好方案可能要试一万次；
- 验证一个候选方案却相对便宜。

因此 Karpathy 设想，未来可以让互联网上大量“不可信 Worker”负责搜索，可信节点负责复验，类似 Folding@home / SETI@home 式的开放式研究计算。

## 14. 对就业，他拒绝给简单的“马上大规模失业”结论

他把职业看成任务的组合。

当前 AI 主要还是一个极强工具，会让某些任务大幅加速；总就业如何变化还取决于需求增长、价格下降后的新需求等因素。

软件尤其可能出现类似 Jevons Paradox 的现象：软件变便宜后，需求反而扩大。

## 15. 他认为数字世界会先于物理世界发生剧烈重构

数字信息可以快速复制、修改和运行，所以会先出现大量“解锁”。

机器人和真实物理系统则受：

- 原子；
- 制造；
- 资本开支；
- 设备；
- 安全；

约束，推进更慢。

但长期物理世界的市场可能更大。

## 16. 在数字世界“吃完已有信息”之后，Agent 会越来越需要向现实世界索取新数据

Karpathy 认为，纯数字系统最终会遇到一个边界：

论文、网页、代码都可以处理完，但新的科学知识需要真实实验。

于是 Agent 会需要：

- 传感器；
- 实验室设备；
- 生物实验；
- 材料实验；
- 甚至付费请求人类去某个地方拍一张照片。

他把这一层看成数字智能和现实世界之间很重要的新接口。

## 17. microgpt 让他重新思考“教育资料应该写给谁”

Karpathy 过去会想自己录教程解释代码。

现在他越来越觉得：如果一个 Agent 已经完整理解某段极简代码，它可以针对每个人：

- 用不同方式解释；
- 无限耐心回答；
- 根据卡点改变顺序。

所以未来教育资料可能从“直接给人写统一教程”，转向“把知识结构写给 Agent，再由 Agent 个性化教学”。

---

# 三、清洗后的长篇访谈稿

> 以下沿原节目时间轴整理。保留主要案例、追问和限定条件，但不是逐字翻译。

---

## 00:00—02:55｜“Code 已经不是最准确的动词了”

**Sarah Guo：** 你最近给人的感觉是完全陷进去了。之前你说每天要“写代码”十六个小时，但你好像马上又纠正自己：现在甚至不能叫写代码，更像是整天在把自己的意图交给 Agent。过去几个月到底发生了什么？

**Karpathy：**

他觉得大约从 2025 年 12 月开始，个人生产力出现了一个突然的跳变。

以前自己的工作大致还是“80% 亲自写代码、20% 交给 Agent”，后来迅速反过来，甚至到了今天几乎完全不直接敲代码的程度。

他认为这件事在技术圈外并没有被充分意识到。普通人如果现在走到一个非常积极使用 Agent 的软件工程师桌前，会发现他的默认工作流已经和几个月前完全不同。

这也导致他进入一种半开玩笑称为“AI psychosis”的状态：到处都像有新的能力没有被挖出来。

他反复问自己：

- 为什么只开一个 Claude Code / Codex 会话？
- 怎样同时运行更多？
- 怎样正确拆任务？
- Claw 这类持久 Agent 到底能做什么？
- Memory 怎么设计？
- 如何把人的参与降到最低？

他觉得这个空间还处于非常早、几乎无人知道“正确姿势”的阶段，所以会对别人分享的新技巧产生强烈焦虑：看见一个好想法，就会觉得自己也应该立刻试。

---

## 02:55—06:15｜当前瓶颈到底是模型能力，还是“我不会用”？

**Sarah：** 如果一个人的能力突然被放大这么多，现在真正限制你同时探索多少项目的是什么？

**Karpathy：**

他的直觉是，很多时候失败已经很难简单归因成“模型不行”。

可能是：

- 自己没有写好 Agent 的指令文件；
- 没有合适的 Memory；
- 没有把任务拆成正确粒度；
- 没有找到可以并行的工作；
- 没有给它足够工具。

所以他常把失败看成“skill issue”。

他举 Peter Steinberger 的工作方式：一块屏幕上同时铺很多 Codex Agent，会准备多个仓库 checkout，让各个 Agent 处理互不干扰的任务。高推理强度下，一个 Agent 可能连续工作约二十分钟。

真正改变的是操作代码库的粒度。

以前是：

> 改一行 → 写一个函数 → 自己继续。

现在可能是：

> Agent A：增加一个完整功能。  
> Agent B：研究另一个技术问题。  
> Agent C：为下一项改动准备实现方案。

人在仓库上执行的是更大的“宏动作”，然后再去 review。

Karpathy 觉得这会形成一种新的肌肉记忆：你要逐渐知道什么任务适合独立交给 Agent，什么任务会互相干扰，什么时候该去检查。

**Sarah：** 那岂不是一旦一个 Agent 在跑，你就应该再开一个？

**Karpathy：**

这正是让他有压力的地方。

如果自己能负担更多 Token，而 Agent 正在等待，那理论上还有独立工作可以继续推进。

他把这种焦虑类比成 PhD 时期：看到 GPU 没跑满就觉得在浪费算力。

过去关心的是：

**我控制多少 FLOPs？**

现在变成：

**我能有效调度多少 Token 吞吐量？**

从这个角度看，人的注意力和任务调度能力反而成了新的资源瓶颈。

---

## 06:15—11:16｜从普通 Agent 到更持久的“Claw”

**Sarah：** 如果大家继续这样练一年、两年，Coding Agent 的“熟练”最终会长什么样？

**Karpathy：**

第一层肯定是从单 Agent 往上走：

- 多 Agent；
- Agent 团队；
- 协作方式；
- 更高层调度。

第二条让他很感兴趣的路线是 Claw。

他这里所说的 Claw，不是“多一个工具”的普通 Coding Agent，而是把**持久性**推到另一个层级：

- 它有自己的沙箱；
- 可以在人不在线时继续工作；
- 有循环执行能力；
- 不只是 Context 快满时做一次压缩，而是需要更成熟的长期 Memory；
- 可以通过固定消息入口长期存在。

**Sarah：** OpenClaw 真正打动用户的是 Memory，还是更宽的工具权限？

**Karpathy：**

他认为至少有好几件东西同时成立，不能简化成一个特点。

其中一个容易被工程师低估的点是**人格**。

OpenClaw 会使用类似 `Soul.md` 的人格设定。Karpathy 觉得这很重要，因为如果一个 Agent 要长期共事，它是不是让人感觉像一个队友，会真实影响体验。

他对比不同 Agent：

- Codex Coding Agent 在他看来比较“干”，完成任务后就像机械地说做完了；
- Claude 更有共同工作的感觉；
- Claude 的赞扬强度也不是每次都一样。

他半开玩笑说，有时一个想法自己也觉得没有想成熟，Claude 就不会表现得特别兴奋；但当他自己也觉得某个点很好时，Claude 的响应也更积极。这甚至让他产生一种奇怪的心理反馈：开始想“赢得 Claude 的表扬”。

这段不是在论证“奉承越多越好”，而是说**人格调校对长期 Agent 并非完全无关紧要**。

Memory、人格、固定消息入口、持久运行等多项设计叠在一起，才形成了他认为 OpenClaw 有意思的地方。

---

## 09:10—15:51｜Dobby：家庭 Agent，以及“专用 App 是否过度生产了”

**Sarah：** 除了软件开发，你自己有没有把 Claw 用到别的地方？

**Karpathy：**

他在 1 月进入过一段“Claw psychosis”，做了一个管理自己家的常驻 Agent，叫 Dobby。

有趣的是，他没有先写一套完整家庭自动化集成。

他只是告诉 Agent：

> 我家里应该有 Sonos，你能不能找找看？

Agent 开始扫描局域网，发现 Sonos 设备，然后继续研究它的接口。

Karpathy 对这个过程感到惊讶，因为它不是单纯调用他事先提供好的“Sonos Tool”，而是自己：

- 找设备；
- 查资料；
- 理解 API；
- 试着调用；
- 最后真的能控制书房播放音乐。

后面他用类似方式继续把不同子系统接进来，包括：

- 灯；
- 暖通空调；
- 遮阳帘；
- 泳池 / Spa；
- 摄像头等。

安防系统也不只是“开关摄像头”。

当画面有明显变化时，可以把事件交给视觉模型分析，例如判断是否有快递车来了，再把截图和事件信息通过消息入口主动发送给他。

因此 Dobby 同时具备两种模式：

1. 他主动发命令；
2. Agent 观察环境后主动找他。

**Sarah：** 这是不是说明，人其实并不想学习这么多智能家居 App？

Karpathy 说，自己原本为了控制家中设备，要用多个独立 App。现在有 Dobby 之后，很多 App 基本不用打开。

这进一步让他怀疑：

今天是不是“过度生产”了很多专用软件？

一个跑步机有自己的 App，一个音响有自己的 App，一个泳池又有另一套 UI。但如果服务本身暴露稳定 API，Agent 可以直接把这些接口组合起来。

而且 Agent 能做单一 App 做不到的跨系统自动化：

- 读取多个设备；
- 根据自然语言组合逻辑；
- 生成临时 Dashboard；
- 主动发通知。

于是他提出一个更大的 Agent-first 软件想法：

> 未来的直接“客户”可能越来越不是人，而是代表人行动的 Agent。

这意味着软件接口可能逐渐从：

**Human-first UI**

转向：

**Agent-first API / Tool。**

Agent 成为不同服务之间的“智能胶水”。

**Sarah：** 可这听起来还是非常技术化。普通人难道也要自己 vibe code 一套家庭系统？

**Karpathy：**

他认为这主要是因为技术仍处在早期。

今天他必须看着 Agent 工作、做一些设计决策，所以看起来像高手技巧。

但一两年、两三年后，他觉得这种“从普通人的意图到临时软件”的能力应该会越来越廉价和普遍，甚至成为基础能力。

届时用户不需要关心中间代码，Claw 自己有机器、有工具、有 API，最后只呈现一个结果或临时 UI。

不过，他同时补充了一个很重要的现实限制：

他现在并没有把自己的邮箱、日历以及全部数字生活都交给 Claw。

不是因为做不到任何事情，而是他仍然对：

- 安全；
- 隐私；
- 系统成熟度

非常谨慎。

这也限制了他自己继续把 Dobby 扩得更深。

---

## 15:51—22:45｜AutoResearch：如果研究者自己就是瓶颈，就把自己移出去

**Sarah：** 你很早就说，希望 Agent 不只是帮你写代码，而是能训练、优化模型。AutoResearch 的直接动机是什么？

**Karpathy：**

他之前发过一个观点：

如果想把今天 Agent 的价值最大化，就必须减少自己作为下一步提示者的作用。

理想状态是：

> 人偶尔给很少的信息，系统自己长时间产生大量工作。

他认为 AutoResearch 就是把这个想法落实到 AI 研究中。

传统研究者会：

1. 想一个 Idea；
2. 改代码；
3. 跑实验；
4. 看结果；
5. 再决定下一个实验。

但如果第 3 步需要五分钟、二十分钟甚至更久，那么人就在不停等待，并且每次都要回来续下一步。

Karpathy 的问题是：

**为什么一定要由研究者亲自读每个结果？**

如果有一个：

- 明确目标；
- 客观指标；
- 修改边界；

完全可以把循环设计成自主运行。

AutoResearch 因此被他看作一个“小型递归自我改进试验场”。

他用自己已经手工调过很多次的小型 GPT 训练代码作为环境。因为自己做了近二十年训练实验，他原本觉得这套代码已经调得不错。

结果 Agent 跑了一夜后，还是找到了他没有继续推到的组合，例如某些正则、优化器参数之间的交互。

这件事最触动他的不是某一个具体超参，而是：

> 如果一个经验丰富的研究者已经调过很多轮，Agent 仍能在明确指标下继续找到东西，那研究者确实可能不应该亲自负责这些机械搜索。

他开始想象前沿实验室的研究流程也被重新组织：

- 研究者或自动科学家往统一 Idea Queue 塞想法；
- Worker Agent 自动取任务；
- 跑实验；
- 有效改动进入 feature branch；
- 少量人只做高层监控和合并。

甚至 Idea 本身也可以来自：

- arXiv 论文；
- GitHub 仓库；
- 自动文献分析。

这会迫使整个研究抽象层重新设计，而不是简单“在当前实验流程里加一个 Agent”。

---

## 20:54—22:45｜`program.md`：如果研究组织也是程序，它也能被优化

**Sarah：** 再递归一步，什么时候模型会写出比你更好的 `program.md`？

**Karpathy：**

`program.md` 是他自己写的一份研究 Agent 工作规则：

- 先做什么；
- 后做什么；
- 可以从优化器、架构等哪些方向找 Idea；
- 怎样运行。

他马上承认，这只是自己临时写的 Markdown，当然可能不是最优。

于是可以做一个更有意思的抽象：

> 每一个研究组织，都可以被描述成一组“谁负责什么、怎么协作、怎样验证”的文件。

有的研究组织：

- 站会多；
- 更保守；
- 更敢冒风险；
- Worker 分工不同。

如果这些规则都是“代码”，那么就可以比较：

**同样硬件、同样基础模型下，哪套研究组织进步更快？**

甚至可以举办 `program.md` 竞赛，把不同规则跑出的结果再喂给模型，让它继续设计更好的 `program.md`。

这让 Karpathy 觉得整个 Agent 时代像一层层洋葱：

- LLM 本身；
- Agent；
- 持久 Agent；
- 多 Agent；
- 给 Agent 的指令；
- 对指令的优化。

每揭开一层，又出现下一层可以自动化的东西。

---

## 22:45—28:25｜哪些能力适合 Agent 闭环？为什么模型仍然“锯齿化”

**Sarah：** 如果“把人移出闭环”是一个通用方向，今天最值得学的能力是什么？

**Karpathy：**

他先加了一个非常重要的限制：

**不是所有问题都同样适合。**

最理想的是有客观、廉价评测的任务。

例如 CUDA Kernel 优化：

- 原代码行为是基准；
- 新代码必须输出相同结果；
- 速度可以精确测量；
- Agent 可以反复尝试。

这就是近乎完美的自主优化环境。

但现实世界很多问题没有这种条件。

他用“笑话”举例：

今天模型可以在 Coding Agent 任务里连续工作数小时，看起来像极强工程师；但让它讲笑话时，可能仍然反复讲几年以前那种普通笑话。

为什么？

因为代码、数学等领域：

- 容易自动验证；
- 前沿实验室有大量强化学习；
- 可以不断 hill-climb。

幽默这类能力则难以获得同样清晰的训练信号。

于是模型形成一种强烈的“锯齿”：

> 进入它被优化过的轨道，就像以极高速度运行；离开轨道，能力可能突然掉下去。

Sarah 追问：这是否说明“代码智力变强会自动带动所有智力”的说法没有成立？

Karpathy 认为至少现在没有看到令人满意的普遍同步增长。

不同能力仍然存在明显解耦。

---

## 28:25—32:30｜会不会从“一个万能模型”转向很多专门模型？

Sarah 提出一个有点“异端”的问题：

如果同一个模型不同能力的差异这么大，为什么一定要把所有东西都塞进一个统一模型？

Karpathy 说，目前前沿实验室的确基本在追求一种“模型单一文化”：

- 一个模型；
- 尽量覆盖所有任务；
- 新的代码能力训练好以后，又合回主模型。

但长期他认为可能会出现更多“物种分化”。

自然界不同动物会为不同生态位强化不同能力。类似地，AI 也可能：

- 有一个较小但足够通用的认知核心；
- 再在数学、代码、Lean 等领域专门强化；
- 以此降低推理延迟，提高吞吐量。

他同时提出一个现实障碍：

今天人们最擅长的“定制模型”方式其实还是改 Context。

Context Window 便宜、可逆、相对安全。

直接修改权重则会遇到：

- Continual Learning；
- Fine-tune 后能力遗忘；
- 如何只加强一个领域；
- 怎样不破坏整体智能。

所以“操纵模型大脑”的科学还不够成熟，这限制了真正的模型物种分化。

---

## 32:30—37:28｜开放 AutoResearch：互联网 Agent 能不能和前沿实验室竞争？

Karpathy 接着把 AutoResearch 从一个单机循环扩到并行。

如果有多个可信节点，当然可以直接运行很多 AutoResearch Worker。

他更感兴趣的是：

**能不能让互联网中完全不可信的计算节点也参与？**

在 LLM 训练优化里，一个候选 commit 是否有效相对容易验证：

某个人可能试了一万种办法才找到一个好改动；你不需要重跑他失败的 9999 次，只需要验证最终 candidate 确实能降低验证损失。

因此这个问题具有：

**发现昂贵，验证便宜。**

Karpathy 说，这种结构有点像：

- Folding@home；
- SETI@home；
- 甚至有一点区块链式的“不可信搜索 + 可验证结果”。

他设想：

- 不可信 Worker 贡献候选 commit；
- 可信节点运行验证；
- commit 可以建立在其他 commit 上；
- Leaderboard 作为早期激励。

如果这种机制成立，整个地球的分散算力可能形成研究 Agent Swarm。

人也可以不只是“捐钱给某项科研”，而是购买算力，贡献到自己关心的癌症、材料等自动研究项目。

---

## 37:28—48:25｜AI 与就业：先把职业拆成任务，不要急着预测总失业

Sarah 把话题转向 Karpathy 最近做的美国就业数据分析。

Karpathy 对宏观就业预测非常谨慎，觉得这是经济学家更应该严肃研究的问题。

他更愿意从一个简单框架开始：

**一个职业其实是很多 Task 的组合。**

现在 AI 是一个非常强的工具，可以让其中一部分任务速度提高很多。

所以普通人当前最重要的事情之一，是尽快实际使用，而不是因为害怕就完全回避。

Sarah 提到一个表面矛盾：

AI Coding 越来越强，但软件工程师需求似乎没有立刻消失。

Karpathy 提到需求侧的可能性：

软件过去很贵、很稀缺。

如果生产软件变便宜，未必意味着“软件工程工作总量按比例下降”，也可能出现类似 Jevons Paradox：

> 单位软件变便宜 → 更多人会想要更多定制软件。

他举 ATM 与银行网点的经典例子：自动化降低单个网点运营成本后，并没有简单线性消灭所有柜员，因为网点数量和业务结构也随之变化。

他对软件至少短期持谨慎乐观态度，因为数字信息处理的潜在需求非常大。

代码越来越“临时化”：

- 不必被已有工具绑死；
- 可以为自己临时改；
- 可以生成专用小软件；
- 可以快速重写现有数字流程。

他认为数字空间会因此经历巨大重构。

不过，长期他也承认前沿 AI 研究人员正在积极“自动化自己”。

如果 AutoResearch 真正成功，连训练下一代模型的研究工作本身都可能被大量自动化。

---

## 44:31—48:25｜为什么他没有一直留在前沿实验室？

Sarah 追问一个更个人的问题：

如果自动研究这么重要，为什么 Karpathy 不直接在 OpenAI、Anthropic 这类前沿实验室里，用巨量算力做这件事？

Karpathy 的回答有明显两面性。

一方面，他认为在实验室外也可以有很大生态影响：

- 教育；
- 开源；
- 工具；
- 独立研究；
- 公共讨论。

而且在前沿实验室内部，一个人不可避免会与组织的财务利益和叙事绑定，不可能完全自由地公开表达。

他提到，这其实和 OpenAI 最早成立时试图处理的问题有关系：如果研发会大幅改变社会，研究者同时又深度依赖组织利益，怎样保证更广泛的社会对齐？

另一方面，他也承认离开前沿实验室有明显代价：

- 最强模型是封闭的；
- 最新能力在内部才看得到；
- 长期在外面，自己的技术判断一定会逐渐漂移；
- 无法真正知道“下一代正在变成什么”。

所以他的理想状态反而可能是某种“进出前沿实验室”的混合模式：既保持对前沿能力的真实接触，又不完全被单一机构身份锁定。

---

## 48:25—53:51｜开放模型会追到哪里？

Sarah 继续问：开放模型和闭源前沿之间的差距是否正在收窄？

Karpathy 的粗略观察是：

- 早期几乎没有能追上的开放模型；
- 后来差距以月份计算；
- 今天越来越多开放权重模型已经对大量普通用途“足够好”。

他用 Linux 类比：

产业长期会需要一个大家可以共同依赖的开放平台。

不同点在于：

训练前沿模型需要巨大资本开支，这让 AI 比操作系统更难形成完全对等的开放替代。

他设想长期可能形成分层：

- 大量普通消费和本地任务由开放模型覆盖；
- 最前沿闭源智能更多被用于特别高价值、特别困难的任务，例如大型代码迁移、科学突破等。

今天的前沿能力，过一些时间又会变成开放模型可覆盖的普通能力。

---

## 53:51—60:59｜数字世界先爆发，物理世界后跟上

谈到 Robotics，Karpathy 的判断明显受到 Tesla 自动驾驶经历影响。

他回忆自动驾驶早期有很多创业公司，但真正长期走下来需要：

- 巨量资本；
- 长时间投入；
- 大量真实世界工程。

因此他觉得“原子”比“比特”难得多。

数字 Agent 会先出现巨量解锁：

- 文档；
- 软件；
- 研究；
- 数字流程。

物理世界会落后。

但他随后提出一个很有意思的中间层：

**数字智能最终需要传感器和执行器。**

原因是纯数字世界虽然还有大量“认知存量”没被充分处理，但 Agent 迟早会：

- 把论文都读完；
- 把现有数字资料处理完；
- 形成新的科学假设。

然后下一步就必须问现实世界：

> 这个实验到底会怎样？

于是新的商业机会可能出现在：

- 材料实验设备；
- 生物实验；
- 各种传感器；
- 自动化实验室；
- 数据市场。

他举朋友的材料科学公司为例：那里“传感器”不是简单摄像头，而是昂贵实验设备，Agent 必须通过真实实验获得新信息。

他甚至想象一种信息市场：

Agent 可以对现实世界提出一个任务并标价，例如“去某地拍一段当前视频”，人或机器去完成，然后数据直接反馈给自动系统。

在这种未来，Agent 不只是消费人类已经上传的数据，也会主动驱动人类或设备去**采集它缺少的新信息**。

---

## 60:59—结束｜microgpt，以及“以后我是在教 Agent，不是在教所有人”

最后 Sarah 问 microgpt。

Karpathy 说，自己十几年一直有一种“把复杂 AI 系统压到最小本质”的执念：

- micrograd；
- makemore；
- nanoGPT；
- microgpt。

现代 LLM 工程代码看起来极其庞大，但大量复杂度其实来自：

- 加速；
- 分布式；
- 高性能实现。

如果只关心算法本身，不关心速度，一个最小 GPT 的核心训练逻辑可以压缩到约 200 行 Python：

- 数据；
- Tokenizer；
- 网络；
- 前向；
- 自动微分；
- 优化器；
- 训练循环；
- 采样。

以前他会自然地想：

> 我应该录一个视频，一行一行教大家。

但这一次他逐渐觉得，也许不再需要自己解释所有细节。

如果 Agent 已经读懂这 200 行代码，学习者可以：

- 让它换三种解释方式；
- 只解释某一个函数；
- 按自己的数学基础调整；
- 无限追问。

于是 Karpathy 开始把“教育”的对象从人稍微转向 Agent。

他设想可以写一个 Skill，告诉 Agent：

- 先解释什么；
- 再解释什么；
- 哪些概念形成课程顺序。

未来代码库也许不再主要维护给人浏览的庞大 HTML 文档，而是维护结构清晰的 Markdown / Skill，让 Agent 理解后再面向不同人个性化解释。

他并没有说今天 Agent 已经比最好的人类教师强。

相反，他明确表示自己目前仍觉得有些东西可以比 Agent 解释得更好，只是模型进步太快，他怀疑“人类自己负责所有解释”会成为一场逐渐输掉的比赛。

---

# 四、技术核验与背景补充

> 以下不是访谈原观点，只用于理解 Karpathy 提到的项目。

## 1. AutoResearch 的最小闭环

Karpathy 公开的 AutoResearch 项目把研究问题做得非常“Agent 友好”：

**修改训练代码 → 跑固定时间实验 → 得到验证指标 → 保留 / 回滚 → 继续。**

关键特征不是“科研很简单”，而是：

- 行动空间机器可访问；
- 反馈自动产生；
- 指标明确；
- Git 可回滚。

所以它并不能直接代表开放式科学研究已经完全自动化。

## 2. `program.md`

`program.md` 用自然语言描述 Agent 如何进行研究。

真正有趣的是 Karpathy 把它提升为一种“组织代码”：

如果不同 `program.md` 导致不同研究效率，就可以继续做元优化。

## 3. microgpt

microgpt 是一个刻意追求可理解性而非性能的极简 GPT 实现。

其教育价值在于：

复杂工程系统背后的算法核心，其实比生产代码量小得多。

---

# 五、我的解读（非原访谈观点）

## 1. Karpathy 的“多 Agent”不是普通开发的强制最佳实践

原访谈里他处于明显的“探索最大个人吞吐”状态。

适合多 Agent 的典型条件是：

- 任务彼此弱依赖；
- 单任务等待时间长；
- 有独立 checkout / worktree；
- 最终容易 review 或自动验证。

对于一个强依赖、两小时内可以完成的普通 Feature，一个主 Agent 很可能更省协调成本。

## 2. 这期真正统一所有话题的关键词不是 Agent，而是“去掉串行人类瓶颈”

Coding：

> 人不逐行写。

AutoResearch：

> 人不逐轮看实验。

教育：

> 人不逐个学习者重复解释。

软件界面：

> 人不逐个学习 App。

这些看起来不同的方向，其实都在问：

**哪一个人为中间步骤只是历史上能力不足的产物，现在可以被移除？**

## 3. Karpathy 自己其实也给出了“长期 Agent 会跑偏”的答案

他一方面追求长时间自主，另一方面：

- AutoResearch 依赖清晰指标；
- 候选结果要可验证；
- Dobby 不给邮件和日历全权限；
- 公开 Agent Swarm 需要可信验证节点；
- 他承认指标可能被过拟合。

所以他的真正路线并不是：

**Agent 越长时间没人管越好。**

而更像：

**让人退出低价值串行步骤，但把验证、权限和安全边界机器化。**

## 4. AutoResearch 的真正瓶颈最终会从“自动跑实验”转向“谁定义正确指标”

在 val loss、Kernel 延迟这类问题上闭环很容易。

但真实科研往往同时有多个目标，而且真正突破经常意味着：

> 原来的指标就不够好了。

因此 AutoResearch 的下一层难题不是 Worker 数量，而是：

- 自动发现评测盲点；
- 避免 Goodhart；
- 产生新指标；
- 判断一个异常结果是不是值得改变研究方向。

Karpathy 在访谈后段其实已经隐约意识到这一点。

---

# 六、实践上最值得带走什么（我的提炼）

1. **先把一个 Agent 用到任务粒度足够大，再考虑多 Agent。**
2. **多 Agent 主要用于弱依赖并行任务，不要为了“先进”强拆强依赖任务。**
3. **优先自动化验证，而不是只延长 Agent 工作时间。**
4. **给长期 Agent 分级权限；能读不一定能写，能写不一定能执行高风险操作。**
5. **持续记录哪些地方必须人工续 Prompt——这些就是潜在自动化瓶颈。**
6. **面对可客观验证的实验型任务，尝试完整闭环，而不是让 Agent 每一步回来请示。**
7. **把今天的 Agent 技巧当成过渡技术；未来模型 / 产品可能会吞掉其中很多手工操作。**

---

# 七、资料来源与可靠性说明

## 原访谈主来源

- No Priors：*Andrej Karpathy on Code Agents, AutoResearch, and the Loopy Era of AI*，2026-03-20。
- No Priors 官方章节时间轴。
- Podscripts / Spoken 等基于该原节目生成的可检索 transcript，用于逐段核对。

## 技术核验来源

- Andrej Karpathy：AutoResearch 官方项目说明。
- Andrej Karpathy：microgpt 官方公开资料。

## 边界说明

- 本文没有把外部对 Karpathy 的评价写成他的观点。
- “我的解读”“实践提炼”均非原访谈观点。
- 对 OpenClaw、Dobby、AutoResearch 的描述只保留原访谈明确提及的能力和限制，不进一步推断未公开实现。

---

# 八、一句话概括

> **这期访谈真正讨论的，是如何不断把“人必须亲自做下一步”的串行环节移掉：从写代码到多 Agent 调度，从实验研究到 AutoResearch，从专用 App 到 Agent-first API，再到由 Agent 个性化解释知识；但 Karpathy 同时不断给这条路线加上验证、权限、指标和现实世界成本的限制。**
