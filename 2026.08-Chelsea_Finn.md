# Chelsea Finn：2026 机器人前沿状态——要点与清洗编辑版长篇稿

> 节目：Y Combinator Startup Podcast / Startup School 2026  
> 原题：Chelsea Finn: This is the State of the Art in Robotics  
> 发布时间：2026 年 8 月 13 日  
> 时长：约 58 分钟  
> 形式：主题演讲 + Q&A  
> 嘉宾：Chelsea Finn，Physical Intelligence 联合创始人、Stanford 教授

---

# 一、文档说明

这不是对原始演讲逐字逐句的翻译，而是一份基于 YC / Root Access 完整 transcript，并结合 Physical Intelligence 在 2025—2026 年公开的 π*0.6 / RECAP、MEM、RLT、π0.7 等技术资料交叉核验后的中文编辑稿。

整理时做了以下处理：

1. 删除开场寒暄、招聘宣传、重复描述和视频演示中无法脱离画面理解的细节。
2. 保留演讲的原始技术主线：可靠性 → RL → 长时自主 → Memory → Generalist → Compositional Generalization。
3. Q&A 部分按技术主题重新组织，而不是机械保持问答顺序。
4. 对“13 小时运行”“2× throughput”“90%+ espresso success”等数字以演讲与 PI 官方公开结果相互校正。
5. 对 π0.7、MEM、RLT 等模型能力采用 PI 官方资料的严格定义，避免把演讲中的类比扩大成未公开能力。
6. 文末增加“从 VLA / 数据闭环 / 质检视角的解读”，但与 Chelsea 的原始观点明确区分。

---

# 二、核心观点

## 1. 2026 年机器人真正的难题已经从“能不能做”转向“能不能长期可靠地做”

机器人已经可以折衣、清理厨房、洗油锅、削胡萝卜、做三明治、切蔬菜、制作咖啡。

但 Chelsea 认为这些 Demo 本身已经不是最关键的问题。真正决定机器人是否有实际价值的是：

**Reliability × Speed × Long-term Autonomy。**

一个任务偶尔成功并不等于产品。机器人真正有用，需要做到：

- 长时间不出错；
- 失败频率足够低；
- 速度具有生产价值；
- 不需要人不断 babysit。

## 2. 机器人对可靠性的要求，比 Chatbot 高得多

搜索、推荐、ChatGPT、Coding Agent 都允许人类位于 Decision Loop 中。模型给错答案时，人可以忽略、重新 Prompt、修改、再次尝试。

机器人直接改变物理世界。如果它打翻咖啡、摔坏杯子、折坏包装盒、碰撞设备，就不能简单用“再问一遍”解决。

因此 Physical AI 的价值会更依赖真正的自主运行，也意味着可靠性门槛更高。

## 3. Waymo 给机器人领域最大的启示，是“学习系统真的可以安全地进入物理世界”

Chelsea 把 Waymo 视作非常重要的先例。自动驾驶证明：

**Machine Learning System → Physical World → Autonomous Production Deployment**

并不是理论幻想。

机器人和自动驾驶的任务不同，但 Waymo 至少说明了：当可靠性足够高以后，学习系统可以直接承担真实世界决策，而不只是给人建议。

## 4. 想把成功率从“不错”推到“真正可用”，人工反复调数据不够

传统机器人迭代通常是：

1. 收数据；
2. 训练；
3. Eval；
4. 找失败；
5. 再收边界样本；
6. 重训。

这确实能提高模型，但人会成为迭代频率的瓶颈。

如果要到 99% 甚至更高可靠性，系统需要主动寻找：

- 自己哪里不行；
- 哪些状态需要额外监督；
- 哪些 Failure Mode 值得学习。

这自然把问题带到 Reinforcement Learning / Learning from Experience。

## 5. 把 LLM 的 PPO/GRPO 直接搬到真机并不可行，因为真实 Rollout 太贵

LLM RL 可以生成数百万甚至数千万次尝试，因为一次 Rollout 只是 GPU Compute。

机器人不是这样。

如果一个 Episode 需要一分钟：

**100 万条轨迹 ≈ 700 个 Robot-days。**

而且还没有算 Reset、设备故障、环境恢复、人工准备、物品耗材。

因此 Robotics RL 的核心不是“能否 RL”，而是：

**如何把每一次真实 Rollout 的信息利用率提高几个数量级。**

## 6. RECAP 的第一个关键思想：不要浪费时间让机器人沿着 Dead End 一直失败

如果机器人已经进入明显错误状态，例如一次抓起两个贴在一起的纸箱、衣服越折越乱、物体掉入无法继续操作的位置，继续 Rollout 很可能产生大量无价值轨迹。

Chelsea 展示的方案是 Human Intervention。

人在关键时刻介入：

- 帮它回到可恢复状态；
- 展示怎样 Recovery；
- 或直接提前终止 Episode。

这不是传统意义上的“失败说明模型不好”，而是：

**Failure State → 最有价值的训练入口。**

## 7. 第二个关键思想：训练通用 Value Function，减少同一 Prompt 的重复 Rollout

PPO/GRPO 往往需要对一个 Prompt 采很多次，比较哪些结果更好。现实机器人承担不起这种样本量。

PI 的思路是把价值判断在任务之间摊销：

训练一个 General-purpose Value Model，判断：

- 当前是在向成功推进；
- 还是倒退；
- 大概距离任务完成还有多远。

同一个 Value Model 可以覆盖折衣、冰箱取物、箱体组装、咖啡制作。

这样系统不必在每个任务里重新用大量 Rollout 才估计出“什么状态好”。

## 8. RL 的价值不只是提高 Success Rate，而是同时优化 Throughput

Chelsea 强调机器人产品不能只测成功率。

机器人 A 98% 成功但完成一次要 10 分钟，机器人 B 95% 成功但两分钟完成一次，在真实商业流程里价值完全不同。

所以 PI 使用 Throughput，把：

**Success × Speed**

合并成更接近真实生产价值的指标。

在 RECAP 实验中，从 RL Post-training 阶段本身就观察到约 **2× throughput** 提升。

## 9. Espresso 实验的价值不在于“会做咖啡”，而在于验证 13 小时可靠运行

制作 Espresso 包含很多很不友好的机器人操作：

- 插入 Portafilter；
- 精确对准；
- 等待；
- 处理液体；
- 搬运满杯；
- 防止洒出。

PI 不只展示一次成功视频，而是让系统连续运行约 **13 小时**。

这把评测对象从：

**Can it do it?**

变成：

**Can it keep doing it?**

这是机器人从论文 Demo 到生产系统非常关键的变化。

## 10. 当前 VLA 的另一个新瓶颈开始浮现：Memory

许多机器人 Foundation Model 几乎没有真正历史上下文。

输入通常是当前 Camera、当前 State、当前 Instruction。

这对短技能够用，但对于清理整个厨房、做完整顿饭、多阶段装配，模型必须记住：

- 已经完成什么；
- 哪些东西移动过；
- 哪些子任务还没做；
- 最近失败发生了什么。

因此，当 Motor Skill 越来越强，Memory 开始从次要问题变成主要问题。

## 11. 直接把过去视频塞入 Context Window 不现实

假设机器人：

- 50 Hz；
- 4 路 Camera；
- 每张图约 256 Tokens。

仅仅 10 秒历史，就可能对应约 **50 万 Tokens**。

即便降到每秒一帧，也可能有约 1 万 Tokens，而这还只是十秒。

所以机器人 Long Context 不能靠“暴力扩大文本式 Context Window”解决。

## 12. MEM 使用多时间尺度记忆

PI 的 MEM 把记忆拆成两层。

### Short-term Visual Memory

保留最近几秒的细节：

- 遮挡前物体在哪里；
- 手刚才怎么动；
- 最近发生什么。

采用更高效的视频编码结构压缩。

### Long-term Textual Memory

对于几分钟甚至更久之前的信息，不保留所有 Raw Frame，而转成抽象的文本式记忆，例如：

- 已经把锅放进水槽；
- 冰箱下门还开着；
- 某块台面已经擦过。

这种设计让模型能够完成约 10—15 分钟、多步骤、不重复的任务。

## 13. 机器人“记忆”本质上不是 Storage，而是 Representation

近期动作需要视觉细节，长期历史更多需要语义摘要。

所以真正的 Memory System 要解决：

**What to remember + How to represent it。**

而不是“把所有 Sensor Data 都存下来”。

## 14. Chelsea 认为机器人正从 BERT 时代走向 GPT / DALL-E 时代

她用通用 AI 历史类比机器人发展。

早期 NLP 一个任务一个 Dataset、一个任务一个 Fine-tuned Model。后来有 Pretrained Model，再后来出现 Out-of-the-box Generalist。

机器人直到 2023 年仍大量处在“每个项目重新采数据、重新训模型”的阶段。

2026 年，她认为机器人已经开始进入：

**GPT / DALL-E-like Era。**

## 15. 真正的 Generalist 不能要求每个新任务都 Fine-tune

如果每次部署新任务、新机器人、新场景都要重新采数据再 Fine-tune，那么它仍然不是真正的 General-purpose Model。

所以 π0.7 的重要目标之一是：

**Out-of-the-box performance。**

一个统一模型直接执行大量技能，而不是每个任务一套 Specialist。

## 16. π0.7 的关键数据策略不是只要“高质量数据”，而是 All the Data

训练集合包括：

- 高质量 Teleoperation；
- 低质量 Demonstration；
- Robot Policy Rollout；
- RL 期间产生的 Experience；
- Human Video；
- Web Data；
- 多种机器人。

传统训练很容易把低质量数据删掉。

π0.7 的思路更接近：

**尽量保留信息，但告诉模型这条数据是什么性质。**

## 17. Metadata Prompting 让“低质量数据”从噪声变成可以利用的信息

如果直接混入大量低质量数据，性能可能下降。

π0.7 会额外告诉模型：

- Episode Quality；
- Speed；
- Control Mode；
- Episode Length；
- Strategy；
- Subtask；
- Visual Subgoal。

这样模型不仅知道 **What to do**，还知道 **How this example was done**。

实验中，在没有 Metadata 时加入更多低质量数据会导致性能下降；加入 Metadata Prompting 后，这些数据反而继续提供收益。

## 18. 这意味着“机器人数据清洗”的目标可能从删除坏数据转向给数据补上下文

这是非常重要的数据观变化。

以前：

**Bad Data → Delete。**

π0.7 展示的方向更像：

**Suboptimal Data + Quality Label / Strategy Context → Useful Training Signal。**

不是所有“低质量”轨迹都该被清除。

关键是模型是否知道：

- 它质量低；
- 为什么低；
- 应不应该模仿这种速度/策略。

## 19. π0.7 展示了更强的 Compositional Generalization

Chelsea 用 DALL-E 的“牛油果椅子”解释组合泛化：模型没见过具体组合，但分别理解两个概念，所以可以重组。

在机器人中，他们测试了两种组合。

### Task × Object / Appliance

模型没有专门采集某个 Air Fryer 任务，却能结合：

- 对厨房 Appliance 的语义理解；
- 开关/放置等已有技能；
- Language Coaching

操作新设备。

### Task × Embodiment

某个 UR5e 双臂系统从未采集折衣数据，但统一模型可以把其他机器人上的折衣能力迁移过来。

## 20. π0.7 的 Cross-Embodiment 结果接近“技能和身体的解耦”

这不是说任何陌生机器人都能完全 Zero-shot。

但它出现了一个更强迹象：

模型可以分别学习：

- “怎样折衣”；
- “这个机器人怎样运动”。

然后在没有该组合训练数据时，把两者组合起来。

这与传统一个 Robot × 一个 Skill 数据集的范式差别很大。

## 21. Chelsea 最惊讶的一次 Emergent Behavior，是左右手策略自动对称迁移

在 Pinwheel Assembly 中，训练数据总是右手拿 Pin、左手拿 Paper。

一次执行时，物体位置反了。

机器人自动改成左手拿 Pin、右手拿 Paper，并继续完成插入。

训练集和 Post-training 都没有专门提供这种左右手反转策略。

这说明模型开始学到某种：

**Cross-hand Equivariance / Symmetry。**

## 22. Future Image / “Imagination”有帮助，但 Chelsea 不认为它已经被证明是必需组件

π0.7 可以预测一个未来 Subgoal Image，再让机器人朝那个视觉状态行动。

这可以理解成非常轻量的“想象”。

实验表明确实有提升，但 Chelsea 很谨慎：没有 Future Image 时，模型本身也已经很强。

因此：

**World-model-like imagination 是有效设计选择，但还不能说是唯一关键。**

## 23. 机器人慢，一个原因是示范数据本身就慢

机器人最容易获得的训练数据通常来自 Human Teleoperation。

问题是：**人遥操作机器人非常慢。**

因此 Policy 如果忠实模仿 Demonstration，自然也慢。

解决办法有两类：

1. 采更快的数据；
2. 学会比数据更快。

RLT 已经显示第二条路线可行：针对精细阶段做 Online RL 后，一些操作速度甚至超过 Human Teleoperation。

## 24. RLT 说明 RL 不只有“大规模离线 Post-training”一种形态

RECAP：

- 长任务；
- 多轮采集；
- 大规模重新训练。

RLT：

- 针对任务中最难的一小段；
- 用紧凑 RL Token 表示 VLA 内部状态；
- 小 Actor/Critic；
- 真机 Online 更新；
- 分钟到小时级数据。

这意味着未来 Robot Learning 很可能有多个 RL 时间尺度：

- Foundation-scale RL；
- Task-level Adaptation；
- Fine-grained Online RL。

## 25. Chelsea 对机器人“ChatGPT Moment”的定义，不是用户数突然爆发

物理机器人有天然分发瓶颈。

你不能像 ChatGPT 一样上线一个网址，5 天一百万用户。

机器人必须生产、安装、运维、部署到场景。

因此 Physical AI 的“GPT Moment”更可能是：

**能力范式先发生变化，商业扩散随后逐步发生。**

她认为从模型能力上，机器人已经开始明显进入这个阶段。

## 26. 真正不可替代的数据仍然是 Robot Experience

Human Video、Web Data 都很重要。

但 Chelsea 给了一个很清楚的类比：

看 Roger Federer 打网球，不代表你就会打网球。

机器人必须真正获得：

- 自己本体的动作后果；
- 接触；
- 力；
- 错误；
- Recovery；
- Success。

未来数据很可能越来越多来自：

**机器人自己尝试任务。**

---

# 三、清洗编辑版演讲稿

## 第一章：不要再只看机器人 Demo

**Chelsea：**

过去一年，我们又让机器人学会了很多看起来很酷的任务，例如洗油锅、削胡萝卜、做三明治、切菜。

但我今天更想讨论的不是“还能做什么新 Demo”。

真正的问题是：

怎样让 General-purpose Robot 在真实世界中有用？

这包含两个要求。

第一，General Purpose。

第二，Useful in the Real World。

后者其实比前者更容易被忽视。

## 第二章：物理 AI 对错误的容忍度完全不同

过去很多成功的 Machine Learning 产品，本质上是在给人做 Recommendation。

广告推荐、搜索、Chatbot、Coding Agent，最终都有一个人在看结果。

所以模型偶尔错了，产品仍然可以非常有价值。

机器人不同。

它的 Action 直接改变世界。

真正发挥价值时，我们不会希望旁边始终站个人替它判断。

所以机器人必须达到比普通生成式 AI 高得多的 Reliability。

## 第三章：从 Espresso 看 Reliability

如果我们希望机器人在咖啡店工作，它不能只“成功做过一次 Espresso”。

它要反复完成安装 Portafilter、等待、处理液体、倒奶、把一杯很满的 Latte 平稳放到指定位置。

这些步骤都非常容易失败。

传统机器学习会不断补失败数据。

但人工补数据最终会越来越慢。

所以更理想的是：

让 Robot 自己去尝试，发现它哪里需要更多经验，然后自动改进。

这就是我们开始使用 Reinforcement Learning 的原因。

## 第四章：机器人 RL 不能烧一百万条真实轨迹

语言模型可以用大量 Rollout。

机器人一条轨迹需要真实时间。

假设一条任务一分钟，一百万次尝试就是数百个机器人天。

所以我们必须做 Sample-efficient RL。

第一个策略是避免 Dead End。

如果 Robot 已经进入明显无法恢复的失败状态，就不要再浪费几十秒。

人可以及时介入，展示怎样 Recovery。

这些 Intervention 本身又成为高价值训练数据。

## 第五章：把价值判断跨任务共享

第二个策略，是学习一个比较通用的 Value Function。

它不只针对某个 Prompt 比较十几次 Rollout。

它直接看 Robot Experience，学习：

“现在离完成任务更近还是更远？”

例如折衣时把刚折好的衣服又展开，是负进展。

冰箱任务里把目标物成功拿出来，是正进展。

这种跨任务 Value Estimation 可以明显降低真机 RL 需要的试验次数。

## 第六章：从一次成功变成 13 小时连续工作

使用这种 Experience + Intervention + Value Learning，我们不仅提高了单次成功率。

更重要的是，我们开始看：

Robot 是否能在真实 Workflow 中连续工作。

Espresso Policy 连续运行大约 13 个小时。

我们也把同样方法应用到 Dandelion Chocolate 的纸箱组装和衣物整理。

量化时不只看 Success Rate，还看 Throughput。

因为现实世界关心单位时间到底能完成多少有效工作。

RL 阶段本身可以带来约两倍 Throughput 提升。

## 第七章：新的瓶颈——Memory

现在一些 Motor Skill 已经很强。

但机器人 Foundation Model 普遍没有真正 Context。

要清理整个厨房，就必须知道：

- 哪些柜子已经整理；
- 哪个锅已经洗；
- 纸巾有没有丢；
- 冰箱是否还开着。

直接塞原始视频历史非常昂贵。

所以我们做 Multi-scale Memory。

最近几秒用压缩 Video Memory。

更久之前的信息转成 Textual Summary。

通过这两层，Robot 可以执行十几分钟的复杂多阶段任务。

## 第八章：机器人正在从 Fine-tuning 时代走向 Out-of-the-box 时代

如果回看 NLP，很长一段时间大家都是一个 Task 一个 Model。

BERT 时代已经有 Pretraining，但最佳效果仍然需要任务 Fine-tune。

GPT 之后，模型开始直接 Out of the Box 做大量任务。

机器人几年前还在更早阶段：

每一个新研究项目都重新采 Dataset、重新训练。

我们希望做的，是一个统一模型：

不用为了每一个 Task、Robot、Room 重新训练。

## 第九章：为什么 π0.7 要吃所有数据

要得到 Generalist，就必须使用非常 Diverse 的数据。

我们不仅使用好的 Demonstration，也使用差一些的 Demonstration、Policy Rollout、RL Experience、Human Video、Web Data。

问题是不同数据行为质量差异巨大。

如果直接混在一起，模型会被坏数据拖累。

所以我们会给 Model 更完整的 Prompt：

- 这是高质量还是低质量；
- 速度如何；
- 任务是什么；
- 当前 Subtask 是什么；
- 想达到的 Visual Subgoal 是什么。

这些 Context 帮助模型正确解释数据。

## 第十章：低质量数据也可以有价值

我们做了一个很有意思的 Ablation。

没有 Metadata Prompt 时，加入更多低质量数据，性能反而下降。

有 Metadata 后，同样加入这些低质量数据，性能继续提高。

所以问题不是：

“这条轨迹够不够漂亮？”

而是：

“模型知道不知道该怎样理解这条轨迹？”

这让我们可以利用远比过去更多的数据。

## 第十一章：Compositional Generalization

我们希望 Robot 像 DALL-E 一样，能够重新组合已经学过的概念。

例如一个 Air Fryer 任务几乎没有直接训练数据。

Robot 可以利用对厨房 Appliance 的语义理解、已学会的 Open / Close / Insert、用户的逐步 Language Coaching 去做一个新组合任务。

另一种组合是在 Embodiment 上。

某台 UR5e 双臂 Robot 没有采过折衣数据。

模型却能把其他 Robot 上的 Folding Skill 转移到这台 Robot。

这说明 Task Knowledge 和 Embodiment Knowledge 开始出现一定程度的可组合性。

## 第十二章：一个很小、但让我非常惊讶的 Emergence

我们有一个 Pinwheel Assembly Task。

所有示范里都是右手拿 Pin，左手拿 Paper。

一次执行中，物体刚好落到了反方向。

Robot 自己改成左手拿 Pin、右手拿 Paper，然后继续组装。

它没有见过这样的示范。

这种行为比“又完成一个新 Task”更让我兴奋，因为它说明模型开始学到更抽象的结构。

## 第十三章：我们已经进入机器人的 GPT / DALL-E-like Era

今天已经出现几个过去没有的属性：

- 一个模型控制很多 Robot；
- Out-of-the-box 执行大量任务；
- 新组合能力；
- Cross-Embodiment；
- 长时自主；
- 真实企业开始 Post-train 基础模型。

因此我认为 Physical Intelligence 已经开始进入 Generalist AI 时间线的右侧。

当然离最终机器人还很远。

但技术范式已经明显不同于三年前。

---

# 四、Q&A 技术整理

## 1. 机器人什么时候出现真正 ChatGPT Moment？

**Chelsea：**

能力层面，已经开始接近。

分发层面不会像 ChatGPT 那样突然。

物理设备制造和部署天然更慢。

未来几年更可能看到越来越多机器人进入真实工作流程，而不是某一天网页上线后突然数百万用户。

## 2. 小团队应该什么时候从 Specialist 切换 Generalist？

现在就可以从 Generalist Foundation Model 开始。

对于大多数场景：

**Generalist Base → Application Fine-tuning**

会比从头训练 Specialist 更合理。

只有在非常受限的硬件和算力环境下，大模型推理成本才可能迫使团队选择更小的 Specialist。

## 3. 机器人领域的 Internet-scale Data 到底是什么？

最核心仍然是：

**Robots operating in real-world circumstances。**

初期可以 Teleoperation。

未来更大比例应该来自 Autonomous Attempt、Deployment Experience、Failure、Human Intervention。

同时配合 Human Video、Web Data、Captioned Image。

但真实 Robot Experience 没有完全替代品。

## 4. Robot Foundation Model 会像 LLM 一样被 Open Source 民主化吗？

不确定。

模型本身可以开源。

但机器人还有 Hardware Cost、Embodied Data、Deployment Infrastructure 这些天然门槛。

所以 Physical AI 的开放生态可能不会简单复制 LLM。

## 5. Action Space 是否已经收敛？

Chelsea 认为目前 Joint Position、End-effector 3D Pose 都能很好工作。

也可以进一步预测 Torque、Voltage、Effort。

不同方式各有优缺点。

但至少在 PI 当前系统里，它不是最主要瓶颈。

这也是一个很重要的研究取舍原则：

**优先解决真正卡能力的东西。**

## 6. World Model / Imagination 是不是必需？

π0.7 的 Future Subgoal Image 能带来提升。

但模型不使用它也已经很强。

所以当前证据更接近：

**有用，但不是已证明的必要条件。**

---

# 五、从 VLA / 数据闭环视角的延伸解读

## 1. 机器人数据 QA 正在从“合格/不合格”转向“带条件的数据价值建模”

π0.7 的结果意味着质量不是二值标签。

同一条数据可以用 Quality、Speed、Strategy、Control Modality 来描述。

模型通过条件化学习“什么时候应该模仿它”。

这比简单 Filtering 更强。

## 2. Intervention Data 应该被单独视作一类高价值数据

Human Intervention 直接采在：

**Policy 当前真实失败分布。**

因此它可能比随机再采一批完美 Demonstration 更高效。

数据系统应该显式保存：

- Intervention Start；
- Failure Context；
- Human Recovery；
- Recovered State；
- 后续是否成功。

## 3. Throughput 应进入 VLA 评测主指标

很多 Benchmark 只看 Success Rate。

但真实生产需要：

**Success / Time。**

尤其机器人算法优化可能出现“成功率 +2%，速度 -50%”这种隐藏退化。

因此 Throughput 是连接 Model Metric 和 Business Utility 的关键指标。

## 4. Memory 会带来一套新的数据质检问题

一旦训练 Long-horizon Memory，需要额外检查：

- Memory Summary 是否正确；
- 是否遗漏关键状态；
- 是否产生错误事实；
- Memory 是否过期；
- 是否和当前 Observation 冲突；
- Summary Compression 是否破坏任务信息。

这已经不是普通轨迹质检。

## 5. π0.7 提示了一个新的 Data Scaling 方向：Diversity 的边际价值可能高于纯数量

其 Ablation 显示：

删除最 Diverse 的那部分数据，比随机删除同规模普通数据伤害更大。

这意味着未来数据采购和治理可能不应该只按 Hours 定价。

更合理的价值函数可能考虑：

**Novelty / Coverage / Embodiment / Task / Failure Distribution。**

---

# 六、资料来源与可靠性说明

主要依据：

1. Y Combinator / Root Access：Chelsea Finn on the next decade in robotics，2026-08。
2. Y Combinator Startup Podcast：Chelsea Finn: This is the State of the Art in Robotics，约 58 分钟。
3. Physical Intelligence：π*0.6 / RECAP。
4. Physical Intelligence：VLAs with Long and Short-Term Memory (MEM)，2026-03-03。
5. Physical Intelligence：Precise Manipulation with Efficient Online RL (RLT)，2026-03-19。
6. Physical Intelligence：π0.7: A Steerable Model with Emergent Capabilities，2026-04-16。

其中演讲原意以完整 transcript 为主；论文和 PI 技术页只用于校正模型结构、实验边界和数字，不把后续未在演讲中出现的结论伪装成 Chelsea 当场原话。

---

# 七、一句话概括

> **2026 年 PI 的真正变化，不是机器人又学会了几个新技能，而是开始同时解决三件更接近“产品”的问题：用 RL 把可靠性和速度推高、用 Memory 把自主时长拉长、再用 π0.7 把 Specialist 能力重新压回一个 Out-of-the-box Generalist Model。**
